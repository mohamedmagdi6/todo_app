import 'package:flutter/material.dart';

class ArchivedBody extends StatelessWidget {
  const ArchivedBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
